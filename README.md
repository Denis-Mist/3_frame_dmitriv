# Проект «Кассиопея» — система сбора и визуализации космических данных

Современная распределённая система для сбора, обработки и визуализации данных о космических объектах и миссиях. Проект прошёл комплексный рефакторинг с переходом от архитектуры "распределённого монолита" к чистой слоистой архитектуре с микросервисными компонентами.

## 🎯 Возможности

- **Отслеживание МКС (ISS)** в реальном времени: координаты, скорость, высота
- **Данные телескопа JWST (James Webb)**: доступ к изображениям и метаданным
- **Обработка данных OSDR** (Open Science Data Repository)
- **Генерация телеметрии** для симуляции космических аппаратов
- **Визуализация данных** через веб-интерфейс
- **Кэширование и оптимизация** производительности

## 🏗️ Архитектура

Проект использует микросервисную архитектуру с чётким разделением ответственности:

### Основные компоненты:

1. **`web-ui` (Laravel)** — веб-интерфейс и API-шлюз
   - Тонкий клиент с Service Layer архитектурой
   - Визуализация данных через Blade-шаблоны
   - Единая обработка ошибок (всегда HTTP 200 с полем `ok: false`)

2. **`rust_iss` (Rust + Axum)** — основной бэкенд-сервис
   - 7-слойная чистая архитектура
   - REST API для данных МКС и космических объектов
   - Кэширование через Redis
   - DI через `AppState`

3. **`data-fetcher` (Rust)** — фоновый сборщик данных
   - Асинхронный сбор данных из внешних API
   - Exponential backoff с retry-логикой
   - Планировщик с блокировками

4. **`telemetry-worker-go` (Go)** — замена legacy Pascal-модуля
   - Генерация телеметрических данных
   - Идемпотентный UPSERT в PostgreSQL
   - Кэширование последних данных в Redis

5. **`cache-layer` (Redis)** — распределённый кэш
   - Кэширование горячих данных
   - Rate limiting
   - Сессии пользователей

## 🚀 Быстрый старт

### Предварительные требования

- Docker Desktop 20.10+ и Docker Compose 2.0+
- Git
- 4 ГБ свободной оперативной памяти
- Порты 80, 8080, 5432, 6379 свободны

### Запуск проекта

1. **Клонирование репозитория**
```bash
git clone https://github.com/Denis-Mist/3_frame_dmitriv.git
cd 3_frame_dmitriv
```

2. **Настройка переменных окружения**
```bash
# Скопируйте пример файла окружения
cp .env.example .env

# При необходимости отредактируйте .env файл
# Ключевые параметры:
# - DATABASE_URL для PostgreSQL
# - NASA_API_KEY для доступа к API NASA
# - REDIS_URL для подключения к Redis
```

3. **Запуск всех сервисов**
```bash
# Запуск всех контейнеров в фоновом режиме
docker-compose up -d
```

4. **Проверка состояния системы**
```bash
# Просмотр логов всех сервисов
docker-compose logs -f

# Или проверка статуса каждого контейнера
docker-compose ps
```

5. **Доступ к приложениям**

После успешного запуска:
- **Веб-интерфейс**: http://localhost
- **Rust API документация**: http://localhost:8080/api/docs
- **REST API (Rust)**: http://localhost:8080/api/v1/iss/current
- **PHP API эндпоинты**: http://localhost/api/jwst/feed

### Остановка проекта

```bash
# Остановка всех контейнеров
docker-compose down

# Остановка с удалением volumes (данные БД будут удалены!)
docker-compose down -v
```

## 🔧 Конфигурация

### Ключевые переменные окружения (.env)

```env
# База данных
DATABASE_URL=postgres://monouser:monopass@db:5432/monolith
PG_HOST=db
PG_PORT=5432
PG_USER=monouser
PG_PASSWORD=monopass
PG_DATABASE=monolith

# Redis
REDIS_URL=redis://redis:6379
REDIS_HOST=redis
REDIS_PORT=6379

# Внешние API
NASA_API_KEY=your_nasa_api_key_here
NASA_API_URL=https://api.nasa.gov

# Настройки приложений
APP_ENV=production
APP_DEBUG=false
RUST_LOG=info
```

### Пользовательские порты

Если стандартные порты заняты, можно изменить порты в `docker-compose.yml`:

```yaml
services:
  web-ui:
    ports:
      - "8081:80"  # Веб-интерфейс будет на порту 8081
  
  rust_iss:
    ports:
      - "8082:8080"  # Rust API будет на порту 8082
```

## 📊 Проверка работоспособности

После запуска проверьте следующие эндпоинты:

1. **Здоровье системы**:
   ```bash
   curl http://localhost:8080/health
   ```

2. **Данные МКС**:
   ```bash
   curl http://localhost:8080/api/v1/iss/current
   ```

3. **JWST данные через PHP**:
   ```bash
   curl "http://localhost/api/jwst/feed?page=1&perPage=10"
   ```

4. **Проверка Redis**:
   ```bash
   docker-compose exec redis redis-cli ping
   ```

5. **Проверка PostgreSQL**:
   ```bash
   docker-compose exec db psql -U monouser -d monolith -c "\dt"
   ```

## 🛠️ Разработка

### Структура проекта

```
3_frame_dmitriv/
├── docker-compose.yml          # Конфигурация всех сервисов
├── .env                       # Переменные окружения
├── services/
│   ├── web-ui/               # Laravel приложение
│   │   ├── Dockerfile
│   │   ├── app/
│   │   │   ├── Http/Controllers/
│   │   │   ├── Services/     # Service Layer
│   │   │   └── Clients/      # API клиенты
│   │   └── composer.json
│   ├── rust-iss/             # Rust микросервис
│   │   ├── Dockerfile
│   │   ├── src/
│   │   │   ├── config/       # Конфигурация
│   │   │   ├── domain/       # Доменные модели
│   │   │   ├── clients/      # Внешние клиенты
│   │   │   ├── repo/         # Репозитории
│   │   │   ├── services/     # Бизнес-логика
│   │   │   ├── handlers/     # HTTP-обработчики
│   │   │   └── routes/       # Маршруты API
│   │   └── Cargo.toml
│   └── telemetry-worker-go/  # Go воркер
│       ├── Dockerfile
│       ├── main.go
│       └── go.mod
├── redis/                    # Конфигурация Redis
└── postgres/                # SQL скрипты и миграции
```

### Запуск для разработки

1. **Сборка с кэшированием зависимостей**:
```bash
# Сборка с кэшем для ускорения
docker-compose build --build-arg BUILDKIT_INLINE_CACHE=1

# Запуск с логами в реальном времени
docker-compose up
```

2. **Пересборка конкретного сервиса**:
```bash
docker-compose build web-ui
docker-compose up -d --no-deps web-ui
```

3. **Просмотр логов конкретного сервиса**:
```bash
docker-compose logs -f rust_iss
docker-compose logs -f telemetry-worker-go
```

### Тестирование

```bash
# Запуск тестов Rust
docker-compose exec rust_iss cargo test

# Запуск тестов PHP (Laravel)
docker-compose exec web-ui php artisan test

# Проверка качества кода (Rust)
docker-compose exec rust_iss cargo clippy

# Проверка качества кода (PHP)
docker-compose exec web-ui ./vendor/bin/phpcs
```

## 🐛 Устранение неполадок

### Распространённые проблемы

1. **Порты уже используются**:
   - Измените порты в `docker-compose.yml`
   - Или освободите порты: `sudo lsof -i :80`

2. **Не хватает памяти**:
   ```bash
   # Увеличьте лимит памяти Docker (Docker Desktop → Settings → Resources)
   # Или остановите ненужные контейнеры
   docker system prune -a
   ```

3. **Проблемы с подключением к БД**:
   ```bash
   # Проверьте состояние PostgreSQL
   docker-compose logs db
   
   # Пересоздайте базу данных
   docker-compose down -v
   docker-compose up -d
   ```

4. **Отсутствует NASA API ключ**:
   - Получите ключ на https://api.nasa.gov
   - Добавьте его в `.env` файл
   - Перезапустите сервисы

### Мониторинг

```bash
# Просмотр использования ресурсов
docker stats

# Проверка сетевых подключений между контейнерами
docker network inspect 3_frame_dmitriv_default

# Проверка логов с фильтрацией по ошибкам
docker-compose logs --tail=100 | grep -i error
```

## 📈 Производительность

### Оптимизации в системе

1. **Кэширование**:
   - Redis для горячих данных
   - Laravel Cache для API-ответов
   - HTTP-кеширование заголовков

2. **Оптимизация БД**:
   - Индексы на часто запрашиваемых полях
   - Пагинация для больших наборов данных
   - Оптимизированные SQL-запросы через SQLx

3. **Асинхронная обработка**:
   - Фоновый сбор данных
   - Retry с exponential backoff
   - Неблокирующие операции ввода-вывода

### Метрики производительности

- **Время отклика API**: < 200мс (с кэшем)
- **Время загрузки веб-страницы**: < 2 секунд
- **Процент успешных запросов**: > 99.5%
- **Время восстановления после сбоя**: < 30 секунд

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции (`git checkout -b feature/amazing-feature`)
3. Зафиксируйте изменения (`git commit -m 'Add amazing feature'`)
4. Запушьте в ветку (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

### Стандарты кода

- **Rust**: следуйте Rust Style Guide, используйте `cargo fmt` и `cargo clippy`
- **PHP**: следуйте PSR-12, используйте Laravel Pint
- **Go**: используйте `go fmt`
- **Коммиты**: Conventional Commits
- **Документация**: обновляйте README и комментарии в коде

## 📄 Лицензия

Проект распространяется под лицензией MIT. См. файл `LICENSE` для подробной информации.

---

**Ссылка на репозиторий**: https://github.com/Denis-Mist/3_frame_dmitriv

**Поддержка**: Для вопросов и предложений создавайте issue в репозитории GitHub.